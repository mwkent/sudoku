package com.sudoku;

import org.junit.Assert;

import org.junit.Test;

/**
 * @author Matt Kent
 */
public class PositionTest {

	@Test
	public void test() {
		final Position pos = Position.getInstance(125, -5);
		Assert.assertEquals(row, pos.getRow());
	}

}
